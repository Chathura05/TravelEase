const SupportDashboard = () => {
  return (
    <div className="dashboard-panel">
      <p className="section-kicker">Support</p>
      <h1>Resolve traveler issues from one queue.</h1>
      <p>Support agents can inspect bookings now and can be extended with ticketing next.</p>
    </div>
  );
};

export default SupportDashboard;
